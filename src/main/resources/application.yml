logging.level.org.apache.cayenne.access.QueryLogger: WARN
logging.level.oorg.apache.cayenne.log.JdbcEventLogger: WARN

log4j.logger.org.apache.cayenne.access.QueryLogger: WARN


# ========================
# APPLICATION
# ========================
app:
    myVar: ${VAR:defaultValue}
    ilicachedb: ${ILICACHEDB:/Users/stefan/tmp/ilicache/ilicachedb}
    cloneDirectory: ${CLONE_DIRECTORY:/Users/stefan/tmp/ilicache/}
    cloneFrequency: 0 * * * *
    cloneRepositories:
      - http://models.interlis.ch
      #- http://models.geo.admin.ch
    peerRepositories:
      - https://geo.so.ch/models